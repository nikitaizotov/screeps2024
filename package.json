{
  "dependencies": {
    "grunt-screeps": "^1.5.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@types/lodash": "^4.17.5",
    "@types/node": "^20.14.8",
    "@types/screeps": "^3.3.8",
    "typescript": "^5.5.2"
  },
  "scripts": {
    "local": "rm -Rf dist/* && tsc && node flattenDist.js",
    "build": "rm -Rf dist/* && rm -Rf dist-flat/* && tsc && node flattenDist.js"
  }
}
